services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - JWT_SECRET=change-me
      - DB_PATH=/data/app.db
      - CORS_ORIGIN=*
    volumes:
      - labdata:/data
volumes:
  labdata:
